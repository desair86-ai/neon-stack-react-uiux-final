"use client";
import { useNeonConfigRevision } from './useNeonConfig';

// Dedicated re-export so the architecture matches the recommended file structure.
// The implementation lives in useNeonConfig.js and is re-exported here.
export { useNeonConfigRevision };
