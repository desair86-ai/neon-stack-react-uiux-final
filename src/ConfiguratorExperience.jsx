"use client";
import React,{useEffect,useMemo,useRef,useState} from "react";
import {Header,Footer} from "./components";
import {getConfiguratorOptions} from "./lib/api";
import {AlignCenter,AlignLeft,AlignRight,ArrowLeft,ArrowRight,Check,Crown,Heart,Minus,Moon,Plus,Ruler,RotateCcw,Smile,Sparkles,Star,Sun,Sunset,Trash2,Upload,WandSparkles,Zap} from "lucide-react";
import "./configurator.css";
const STEPS=["text","shapes","color","backboard","hardware"],LABELS={text:"TEXT",shapes:"SHAPES",color:"COLOUR",backboard:"BACKBOARD",hardware:"HARDWARE"};
const COLORS=[{id:"pink",name:"Pink",hex:"#ff2aa8"},{id:"purple",name:"Purple",hex:"#8d3cff"},{id:"blue",name:"Blue",hex:"#198cff"},{id:"cyan",name:"Cyan",hex:"#12dfe5"},{id:"green",name:"Green",hex:"#63df21"},{id:"yellow",name:"Yellow",hex:"#ffd11a"},{id:"orange",name:"Orange",hex:"#ff8618"},{id:"white",name:"White",hex:"#fff"}];
const FALLBACK={options:{colors:COLORS,sizes:[{id:"small",name:"Small",price:5600,description:"39.5 × 10 in"},{id:"medium",name:"Medium",price:9100,description:"51.5 × 13 in"},{id:"large",name:"Large",price:11400,description:"63.5 × 15 in"},{id:"xl",name:"Extra Large",price:14800,description:"87.5 × 17 in"}],shapes:[{id:"heart",name:"Heart",price:300},{id:"star",name:"Star",price:300},{id:"lightning",name:"Lightning",price:300},{id:"crown",name:"Crown",price:300},{id:"moon",name:"Moon",price:300},{id:"smile",name:"Smile",price:300}],backboards:[{id:"cut",name:"Cut to Shape",price:0},{id:"whole",name:"Whole Board / Square",price:1200},{id:"none",name:"No Backing / Minimal",price:0}],hardware:[{id:"screws",name:"Wall Screws",price:0},{id:"wire",name:"Hanging Wire",price:300},{id:"dimmer",name:"Standard Dimmer",price:500},{id:"smart",name:"Smart WiFi / Wireless Remote",price:1000},{id:"indoor",name:"Indoor LED",price:0},{id:"outdoor",name:"IP67 Waterproof Outdoor",price:900}]},fonts:[{id:"neon-script",name:"Neon Script",class:"font-neon-script"},{id:"classic",name:"Classic",class:""}],presentation:{text_color_selection:true,shape_color_mode:"single",shape_color_options:COLORS}};
const BACKGROUNDS=[["Living Room","/images/customneon background (3).png"],["Brick Wall","/images/customneon background (4).png"],["Home Gym","/images/customneon background (5).png"],["Kids Room","/images/customneon background (6).png"],["Office","/images/customneon background (7).png"],["Bar","/images/bar.png"],["Bedroom","/images/bedroom.png"]];
const LIGHTING={night:{label:"Dark Room",filter:"brightness(.38) contrast(1.25)"},evening:{label:"Cozy Evening",filter:"brightness(.62) contrast(1.1) sepia(.12)"},day:{label:"Daytime",filter:"brightness(.95) contrast(1)"}};
const money=v=>`₹${Number(v||0).toLocaleString("en-IN")}`;
const physicalWidth=s=>{const m=String(s?.description||"").match(/([\d.]+)\s*[×x]/);return m?Number(m[1]):50};
const physicalHeight=s=>{const m=String(s?.description||"").match(/[×x]\s*([\d.]+)/);return m?Number(m[1]):10};
const fontFamily=f=>f?.class||f?.family||f?.name||"inherit";
function shapeIcon(name,size=28){const p={size,strokeWidth:1.7},n=String(name||"").toLowerCase();if(n.includes("heart"))return <Heart {...p}/>;if(n.includes("star"))return <Star {...p}/>;if(n.includes("moon"))return <Moon {...p}/>;if(n.includes("crown"))return <Crown {...p}/>;if(n.includes("smile"))return <Smile {...p}/>;if(n.includes("light"))return <Zap {...p}/>;return <Sparkles {...p}/>}
export function ConfiguratorExperience({type="custom_neon"}){
 const mojo=type==="mojo_mix";
 const [config,setConfig]=useState(null),[loading,setLoading]=useState(true),[step,setStep]=useState(0),[text,setText]=useState("Good Vibes Only"),[font,setFont]=useState(null),[align,setAlign]=useState("center"),[size,setSize]=useState(null),[color,setColor]=useState(null),[isMulti,setIsMulti]=useState(false),[letterColors,setLetterColors]=useState({}),[selectedLetter,setSelectedLetter]=useState(null),[shapes,setShapes]=useState([]),[backboard,setBackboard]=useState(null),[hardware,setHardware]=useState(null),[background,setBackground]=useState(BACKGROUNDS[0][1]),[wallFile,setWallFile]=useState(null),[mood,setMood]=useState("day"),[lightOn,setLightOn]=useState(true),[showRuler,setShowRuler]=useState(true),[calibrating,setCalibrating]=useState(false),[calibrationInches,setCalibrationInches]=useState("50"),[calibrationRatio,setCalibrationRatio]=useState(null),[calibrationWidth,setCalibrationWidth]=useState(295),[calibrationPos,setCalibrationPos]=useState({x:.5,y:.52}),[signPos,setSignPos]=useState({x:.5,y:.5}),[fontSize,setFontSize]=useState(80),[bounds,setBounds]=useState(null);
 const previewRef=useRef(null),textRef=useRef(null);
 useEffect(()=>{let active=true;setLoading(true);getConfiguratorOptions(type).then(data=>{if(!active)return;const c=data?.options?data:FALLBACK,o=c.options||FALLBACK.options,fs=c.fonts?.length?c.fonts:FALLBACK.fonts;setConfig(c);setFont(fs[0]);setSize(o.sizes?.[0]);setColor(mojo?null:(o.colors?.[0]||COLORS[0]));setBackboard(o.backboards?.[0]);setHardware(o.hardware?.[0]);setLoading(false)}).catch(()=>{if(!active)return;setConfig(FALLBACK);setFont(FALLBACK.fonts[0]);setSize(FALLBACK.options.sizes[0]);setColor(mojo?null:COLORS[0]);setBackboard(FALLBACK.options.backboards[0]);setHardware(FALLBACK.options.hardware[0]);setLoading(false)});return()=>{active=false}},[type,mojo]);
 const options=config?.options||FALLBACK.options,fonts=config?.fonts?.length?config.fonts:FALLBACK.fonts,presentation=config?.presentation||{},current=STEPS[step],shapeColors=presentation.shape_color_options?.length?presentation.shape_color_options:(options.colors?.length?options.colors:COLORS);
 const valid={text:Boolean(text.trim())&&Boolean(font),shapes:true,color:mojo||Boolean(color),backboard:Boolean(backboard),hardware:Boolean(hardware)},complete=STEPS.every(k=>valid[k]);
 const price=useMemo(()=>Number(size?.price||0)+Number(backboard?.price||0)+Number(hardware?.price||0)+shapes.reduce((n,s)=>n+Number(s.price||0),0),[size,backboard,hardware,shapes]);
 const chars = Math.max(1, (text||"").replace(/\s/g, '').length);
 const spaceCount = ((text||"").match(/ /g) || []).length;
 const linesCount = Math.max(1, (text||"").split('\n').length);
 const baseW = physicalWidth(size), baseH = physicalHeight(size);
 const signW = (chars * (baseW/12)) + (spaceCount * 1.75) + (shapes.length * 6);
 const signH = (linesCount * baseH) + ((linesCount - 1) * 3);
 useEffect(()=>{const box=previewRef.current;if(!box)return;const fit=()=>{const probe=document.createElement("span"),cs=textRef.current?getComputedStyle(textRef.current):null;probe.style.cssText=`position:fixed;left:-99999px;top:-99999px;visibility:hidden;white-space:pre;display:inline-block;font-family:${JSON.stringify(fontFamily(font))};font-weight:${cs?.fontWeight||"400"};letter-spacing:${cs?.letterSpacing||"normal"};line-height:1.02;`;probe.textContent=text||"Preview";document.body.appendChild(probe);const maxWidth=Math.max(140,Math.min(box.clientWidth*.68,calibrationRatio?signW*calibrationRatio:box.clientWidth*.68));const lines=String(text||"").split("\n").length;let low=12,high=190;for(let i=0;i<18;i++){const mid=(low+high)/2;probe.style.fontSize=`${mid}px`;if(probe.scrollWidth<=maxWidth&&probe.scrollHeight<=Math.max(80,box.clientHeight*.44/lines))low=mid;else high=mid}setFontSize(low);document.body.removeChild(probe)};fit();const ro=new ResizeObserver(fit);ro.observe(box);return()=>ro.disconnect()},[text,font,size,calibrationRatio,shapes]);
 useEffect(()=>{const box=previewRef.current,el=textRef.current;if(!box||!el)return;const update=()=>{const a=box.getBoundingClientRect(),r=el.getBoundingClientRect();setBounds({left:r.left-a.left,top:r.top-a.top,width:r.width,height:r.height})};update();const ro=new ResizeObserver(update);ro.observe(el);ro.observe(box);return()=>ro.disconnect()},[fontSize,text,align,signPos,shapes]);
 const addShape=s=>setShapes(prev=>{const l=prev.filter(x=>x.position==="left").length,r=prev.filter(x=>x.position==="right").length;return [...prev,{...s,uid:`${s.id}-${Date.now()}-${Math.random()}`,position:l<=r?"left":"right",color:shapeColors[0]||COLORS[0]}]});
 const removeShape=uid=>setShapes(prev=>prev.filter(s=>s.uid!==uid));
 const updateShape=(uid,patch)=>setShapes(prev=>prev.map(s=>s.uid===uid?{...s,...patch}:s));
 const uploadWall=e=>{const f=e.target.files?.[0];if(!f)return;if(wallFile)URL.revokeObjectURL(wallFile);const u=URL.createObjectURL(f);setWallFile(u);setBackground(u);setCalibrationRatio(null)};
 const chooseBackground=u=>{if(wallFile)URL.revokeObjectURL(wallFile);setWallFile(null);setBackground(u);setCalibrationRatio(null)};
 const reset=()=>{if(wallFile)URL.revokeObjectURL(wallFile);setText("Good Vibes Only");setShapes([]);setBackground(BACKGROUNDS[0][1]);setWallFile(null);setMood("day");setLightOn(true);setShowRuler(true);setCalibrating(false);setCalibrationRatio(null);setCalibrationWidth(295);setCalibrationPos({x:.5,y:.52});setSignPos({x:.5,y:.5});setStep(0)};
 const dragSign=e=>{if(calibrating)return;e.preventDefault();const box=previewRef.current?.getBoundingClientRect();if(!box)return;const sx=e.clientX,sy=e.clientY,ox=signPos.x,oy=signPos.y;const move=ev=>setSignPos({x:Math.max(.08,Math.min(.92,ox+(ev.clientX-sx)/box.width)),y:Math.max(.12,Math.min(.88,oy+(ev.clientY-sy)/box.height))});const up=()=>{window.removeEventListener("pointermove",move);window.removeEventListener("pointerup",up)};window.addEventListener("pointermove",move);window.addEventListener("pointerup",up)};
 const dragCalibration=e=>{if(!calibrating)return;e.preventDefault();const box=previewRef.current?.getBoundingClientRect();if(!box)return;const sx=e.clientX,sy=e.clientY,ox=calibrationPos.x,oy=calibrationPos.y;const move=ev=>setCalibrationPos({x:Math.max(.08,Math.min(.92,ox+(ev.clientX-sx)/box.width)),y:Math.max(.08,Math.min(.92,oy+(ev.clientY-sy)/box.height))});const up=()=>{window.removeEventListener("pointermove",move);window.removeEventListener("pointerup",up)};window.addEventListener("pointermove",move);window.addEventListener("pointerup",up)};
 const setCalibration=()=>{const inches=Number(calibrationInches);if(inches>0)setCalibrationRatio(calibrationWidth/inches);setCalibrating(false);};
 const neonColor=color?.hex||"#63df21",lighting=LIGHTING[mood],leftShapes=shapes.filter(s=>s.position==="left"),rightShapes=shapes.filter(s=>s.position==="right");
 const getShadow=c=>lightOn?`0 0 2px ${c},0 0 4px ${c}`:"none";
 const textStyle={fontFamily:fontFamily(font),fontSize:`${fontSize}px`,lineHeight:1.02,whiteSpace:"pre",display:"inline-block",textAlign:align,color:mojo?"transparent":(isMulti?undefined:neonColor),backgroundImage:mojo?"linear-gradient(90deg,#ffde00,#ff7b00,#ff007b,#c400ff,#00d4ff,#ffde00)":undefined,WebkitBackgroundClip:mojo?"text":undefined,backgroundSize:mojo?"300% 100%":undefined,animation:mojo?"nsMojoSpectrum 3s linear infinite":undefined,textShadow:mojo?"none":(isMulti?undefined:getShadow(neonColor)),filter:lightOn?"none":"brightness(0.5)",opacity:lightOn?1:.9};
 const renderText=()=>{if(mojo||!isMulti)return text||"Preview";return (text||"Preview").split("").map((char,i)=>{const c=letterColors[i]||color,cHex=c?.hex||"#63df21";return <span key={i} onClick={(e)=>{if(isMulti){e.stopPropagation();setSelectedLetter(i)}}} style={{color:cHex,textShadow:getShadow(cHex),cursor:isMulti?"pointer":"inherit",display:"inline-block",transform:isMulti&&selectedLetter===i?"scale(1.1)":"none",transition:"transform 0.2s",zIndex:isMulti&&selectedLetter===i?10:1,position:"relative"}}>{char}</span>})};
 const shapePosition=(s,side,index)=>{const gap=Math.max(44,Math.min(78,fontSize*.55)),textLeft=bounds?.left??(previewRef.current?.clientWidth||800)*.5,textWidth=bounds?.width??200,x=side==="left"?textLeft-gap-index*gap:textLeft+textWidth+gap+index*gap;return {left:x,top:(bounds?.top??200)+(bounds?.height??100)/2,color:mojo?undefined:s.color?.hex||neonColor,backgroundImage:mojo?"linear-gradient(90deg,#ffde00,#ff7b00,#ff007b,#c400ff,#00d4ff,#ffde00)":undefined,WebkitBackgroundClip:mojo?"text":undefined,backgroundSize:mojo?"300% 100%":undefined,animation:mojo?"nsMojoSpectrum 3s linear infinite":undefined,opacity:lightOn?1:.9,transform:"translate(-50%,-50%)",fontSize:Math.max(30,fontSize*.42),filter:lightOn?(mojo?`drop-shadow(0 0 10px #ff007b) drop-shadow(0 0 20px #00d4ff)`:`drop-shadow(0 0 10px ${s.color?.hex||neonColor}) drop-shadow(0 0 25px ${s.color?.hex||neonColor})`):"brightness(0.5)",textShadow:lightOn?(mojo?"none":`0 0 2px #fff,0 0 5px #fff,0 0 10px ${s.color?.hex||neonColor},0 0 40px ${s.color?.hex||neonColor}`):"none"}};
 const ruler=useMemo(()=>{if(!bounds)return null;const gap=Math.max(44,Math.min(78,fontSize*.55)),left=bounds.left-leftShapes.length*gap-gap/2,right=bounds.left+bounds.width+rightShapes.length*gap+gap/2,top=bounds.top-Math.min(24,fontSize*.1),bottom=bounds.top+bounds.height+Math.min(24,fontSize*.1);return {left:Math.max(8,left),top:Math.max(8,top),width:Math.max(100,right-left),height:Math.max(70,bottom-top)}},[bounds,leftShapes.length,rightShapes.length,fontSize]);
 if(loading)return <><Header/><main className="ns-config-loading">Loading your neon builder…</main></>;
 return <><Header/><main className={`ns-configurator${mojo?" ns-mojo":""}`}><section className="ns-builder-heading"><div className="ns-container"><div className="ns-breadcrumb">Home / {mojo?"Mojo Mix":"Custom Neon"}</div><h1>CREATE YOUR <em>{mojo?"MOJO MIX":"CUSTOM NEON"} SIGN</em></h1><p>Design it. See it. Love it. <Heart size={15}/></p><div className="ns-trust-row"><span><WandSparkles/> Live Real-time Preview</span><span><Heart/> Custom Made Just For You</span><span><Sparkles/> Premium Quality &amp; Safe</span><span><Star/> Made in India</span></div></div></section>
<section className="ns-container ns-builder-shell"><div className="ns-config-layout"><aside className="ns-panel ns-controls"><div className="ns-stepper">{STEPS.map((s,i)=><button key={s} className={`${step===i?"active":""} ${valid[s]?"done":""}`} onClick={()=>{if(i<=step||STEPS.slice(0,i).every(k=>valid[k]))setStep(i)}}><span className="ns-step-num">{valid[s]?<Check size={13}/>:i+1}</span><span>{LABELS[s]}</span></button>)}</div><div className="ns-control-body">
{current==="text"&&<><div className="ns-field"><label>YOUR TEXT <small>{text.length}/50</small></label><textarea value={text} maxLength={50} rows={3} onChange={e=>setText(e.target.value)}/><small style={{display:"block",marginTop:6,color:"#8992a5"}}>Press Enter only when you want another line.</small></div><div className="ns-field"><label>FONT STYLE <small>{fonts.length} Fonts</small></label><select value={font?.id||font?.name||""} onChange={e=>setFont(fonts.find(f=>String(f.id||f.name)===e.target.value)||fonts[0])}>{fonts.map(f=><option key={f.id||f.name} value={f.id||f.name}>{f.name}</option>)}</select></div><div className="ns-field"><label>ALIGNMENT</label><div className="ns-align"><button className={align==="left"?"selected":""} onClick={()=>setAlign("left")}><AlignLeft/></button><button className={align==="center"?"selected":""} onClick={()=>setAlign("center")}><AlignCenter/></button><button className={align==="right"?"selected":""} onClick={()=>setAlign("right")}><AlignRight/></button></div></div><div className="ns-field"><label>SIZE</label><div className="ns-option-list">{options.sizes?.map(s=><button key={s.id} className={size?.id===s.id?"selected":""} onClick={()=>setSize(s)}><span><b>{s.name}</b><small>{s.description}</small></span><strong>{money(s.price)}</strong></button>)}</div></div></>}
{current==="shapes"&&<><div className="ns-section-title"><div><b>ADD NEON SHAPES</b><small>Each shape has its own colour and position.</small></div></div><div className="ns-shape-list">{options.shapes?.map(s=>{const count=shapes.filter(x=>x.id===s.id).length;return <div className="ns-shape-row" key={s.id}><span className="ns-shape-label">{shapeIcon(s.name,24)}<b>{s.name}</b></span><button className="ns-icon-btn" onClick={()=>addShape(s)}><Plus size={16}/></button><span className="ns-count">{count}</span><button className="ns-icon-btn" onClick={()=>{const last=[...shapes].reverse().find(x=>x.id===s.id);if(last)removeShape(last.uid)}}><Minus size={16}/></button></div>})}</div>{shapes.length>0&&<div className="ns-shape-configs"><h3>POSITION &amp; COLOUR</h3>{shapes.map((s,i)=><div className="ns-shape-config" key={s.uid}><div className="ns-shape-config-top"><b>{s.name} {i+1}</b><button className="ns-icon-btn" onClick={()=>removeShape(s.uid)}><Trash2 size={15}/></button></div><div className="ns-position"><button className={s.position==="left"?"selected":""} onClick={()=>updateShape(s.uid,{position:"left"})}>Left</button><button className={s.position==="right"?"selected":""} onClick={()=>updateShape(s.uid,{position:"right"})}>Right</button></div><div className="ns-mini-color-row">{shapeColors.map(c=><button key={c.id||c.name} className={s.color?.id===c.id?"selected":""} style={{background:c.hex}} onClick={()=>updateShape(s.uid,{color:c})} title={c.name}/>)}</div></div>)}</div>}</>}
{current==="color"&&!mojo&&<div className="ns-field"><div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:12}}><label style={{margin:0}}>TEXT COLOUR</label><button onClick={()=>{setIsMulti(!isMulti);if(!isMulti&&selectedLetter===null)setSelectedLetter(0)}} style={{background:isMulti?"#8b4cff":"transparent",color:isMulti?"#fff":"#8b4cff",border:"1px solid #8b4cff",borderRadius:6,fontSize:9,padding:"4px 8px",fontWeight:800,cursor:"pointer"}}>{isMulti?"SINGLE COLOUR":"MULTI COLOUR"}</button></div>{isMulti&&<div style={{background:"#0a0d14",padding:12,borderRadius:8,marginBottom:15,border:"1px solid #2a3040"}}><div style={{fontSize:10,color:"#aeb5c4",marginBottom:10}}>Click a letter below, then choose a colour.</div><div style={{display:"flex",flexWrap:"wrap",gap:6}}>{(text||"Preview").split("").map((char,i)=>{if(char.trim()==="")return null;const isSel=selectedLetter===i,c=letterColors[i]||color,cHex=c?.hex||"#63df21";return <button key={i} onClick={()=>setSelectedLetter(i)} style={{width:32,height:32,borderRadius:6,background:isSel?"#752eff":"#161a24",border:isSel?"1px solid #9a6cff":"1px solid #333",color:isSel?"#fff":cHex,fontSize:14,fontWeight:"bold",cursor:"pointer",display:"flex",alignItems:"center",justifyContent:"center"}}>{char}</button>})}</div></div>}<div className="ns-color-grid">{(options.colors?.length?options.colors:COLORS).map(c=>{const isSelected=isMulti?letterColors[selectedLetter]?.id===c.id:color?.id===c.id;return <button key={c.id||c.name} className={isSelected?"selected":""} style={{background:c.hex}} onClick={()=>{if(isMulti){if(selectedLetter!==null)setLetterColors(prev=>({...prev,[selectedLetter]:c}))}else{setColor(c)}}} title={c.name}/>})}</div></div>}
{current==="color"&&mojo&&<div className="ns-field"><label>MOJO SPECTRUM</label><p style={{color:"#aeb5c4",lineHeight:1.6}}>Mojo Mix uses a continuous moving multicolour spectrum. The text and shapes animate independently from Custom Neon colours.</p></div>}
{current==="backboard"&&<div className="ns-field"><label>BACKBOARD</label><div className="ns-option-list">{options.backboards?.map(x=><button key={x.id} className={backboard?.id===x.id?"selected":""} onClick={()=>setBackboard(x)}><span><b>{x.name}</b></span><strong>{money(x.price)}</strong></button>)}</div></div>}
{current==="hardware"&&<div className="ns-field"><label>HARDWARE</label><div className="ns-option-list">{options.hardware?.map(x=><button key={x.id} className={hardware?.id===x.id?"selected":""} onClick={()=>setHardware(x)}><span><b>{x.name}</b></span><strong>{money(x.price)}</strong></button>)}</div></div>}
<div className="ns-summary"><span>Estimated Price</span><strong>{money(price)}</strong></div><button className={`ns-next ${step<STEPS.length-1?"":"ns-add-to-cart"}`} disabled={!valid[current]} onClick={()=>step<STEPS.length-1?setStep(step+1):complete&&alert("Configuration ready. WooCommerce cart wiring will use the validated server-side product mapping.")}>{step<STEPS.length-1?"CONTINUE":"ADD TO CART"}</button></div></aside>
<section className="ns-preview-panel"><div className="ns-preview-toolbar"><div className="ns-preview-title">PREVIEW</div><div className="ns-lighting-buttons">{[["night",<Moon size={14}/>],["evening",<Sunset size={14}/>],["day",<Sun size={14}/>]].map(([k,ic])=><button key={k} className={mood===k?"selected":""} onClick={()=>setMood(k)}>{ic}{LIGHTING[k].label}</button>)}</div><div className="ns-studio-actions"><button className={lightOn?"selected":""} onClick={()=>setLightOn(v=>!v)}>{lightOn?"Light on":"Light off"}</button><button className={showRuler?"selected":""} onClick={()=>setShowRuler(v=>!v)}><Ruler size={14}/>{showRuler?"Hide ruler":"Show ruler"}</button><button onClick={reset}><RotateCcw size={14}/></button></div></div>
<div className="ns-canvas" ref={previewRef}><img className="ns-canvas-background" src={background} alt="Room preview" style={{filter:lighting.filter}}/>{showRuler&&ruler&&<div className="ns-sign-ruler" style={{left:ruler.left,top:ruler.top,width:ruler.width,height:ruler.height}}><div className="ns-sign-ruler-h"><i/><b>{signW.toFixed(2)}&quot;</b><i/></div><div className="ns-sign-ruler-v"><i/><b>{signH.toFixed(2)}&quot;</b><i/></div></div>}<div className="ns-neon-art" style={{left:`${signPos.x*100}%`,top:`${signPos.y*100}%`,transform:"translate(-50%,-50%)",width:"100%",height:"100%",position:"absolute",pointerEvents:"none"}}><div className="ns-art-shapes">{leftShapes.map((s,i)=><span key={s.uid} style={shapePosition(s,"left",i)}>{shapeIcon(s.name,Math.max(30,fontSize*.44))}</span>)}{rightShapes.map((s,i)=><span key={s.uid} style={shapePosition(s,"right",i)}>{shapeIcon(s.name,Math.max(30,fontSize*.44))}</span>)}</div><div ref={textRef} className={`ns-neon-text${mojo?" spectrum":""}`} style={{...textStyle,pointerEvents:"auto",cursor:isMulti?"inherit":"grab",userSelect:"none"}} onPointerDown={e=>{if(!isMulti) dragSign(e)}}>{renderText()}</div></div>{calibrating&&<div className="ns-calibration-live" style={{position:"absolute",inset:0,zIndex:50,pointerEvents:"none"}}><div onPointerDown={dragCalibration} style={{position:"absolute",left:`${calibrationPos.x*100}%`,top:`${calibrationPos.y*100}%`,width:calibrationWidth,height:6,transform:"translate(-50%,-50%)",background:"#ff3355",boxShadow:"0 0 16px rgba(255,51,85,.8)",cursor:"move",pointerEvents:"auto"}}><span style={{position:"absolute",left:"50%",top:-24,transform:"translateX(-50%)",color:"#fff",fontWeight:800,whiteSpace:"nowrap"}}>{Math.round(calibrationWidth)} px — drag over a known object</span><i style={{position:"absolute",left:-8,top:-8,width:22,height:22,borderRadius:"50%",background:"#ff3355"}}/><i style={{position:"absolute",right:-8,top:-8,width:22,height:22,borderRadius:"50%",background:"#ff3355"}}/></div><div style={{position:"absolute",left:12,bottom:12,zIndex:51,padding:12,background:"rgba(5,6,10,.94)",border:"1px solid #752eff",borderRadius:12,pointerEvents:"auto",display:"flex",gap:8,alignItems:"center",flexWrap:"wrap"}}><b style={{color:"#a8f2cc",fontSize:11}}>CALIBRATE ROOM SIZE</b><span style={{color:"#8992a5",fontSize:10}}>Place red line over a real object, then enter its width.</span><input value={calibrationInches} onChange={e=>setCalibrationInches(e.target.value)} type="number" min="1" style={{height:38,width:110,background:"#111",color:"#fff",border:"1px solid #444",borderRadius:7,padding:"0 10px"}}/><button onClick={setCalibration} style={{height:38,background:"#752eff",color:"#fff",border:0,borderRadius:7,padding:"0 15px",fontWeight:800}}>SET SCALE</button><button onClick={()=>setCalibrating(false)} style={{height:38,background:"#161a24",color:"#fff",border:"1px solid #333",borderRadius:7,padding:"0 12px"}}>CANCEL</button></div></div>}<button className="ns-calibrate-button" onClick={()=>setCalibrating(v=>!v)}><Ruler size={15}/>{calibrating?"CLOSE CALIBRATION":"CALIBRATE ROOM SIZE"}</button></div><div className="ns-background-picker"><div className="ns-background-title"><b>ROOM / WALL BACKGROUND</b><label className="ns-upload-wall"><Upload size={14}/> UPLOAD YOUR WALL<input type="file" accept="image/*" onChange={uploadWall}/></label></div><div className="ns-background-grid">{BACKGROUNDS.map(([name,url])=><button key={url} className={background===url?"selected":""} onClick={()=>chooseBackground(url)}><img src={url} alt=""/><span>{name}</span></button>)}</div></div></section></div></section></main><Footer/></>;
}
